/target
**/*.rs.bk

# Real activity data dumped from the v1 agent + GitHub. Local only, never committed.
corpus/
*.jsonl

# Model weights (fetched, not committed).
*.onnx
models/

# Derived projections + run outputs (rebuildable).
index/
clusters.json
unit_clusters.json
clusters.md
summary.md
eval_runs.md
cluster.err

.synty/
tmp/
.DS_Store

# Local-only agent-harness state (not committed). AGENTS.md / CLAUDE.md live at
# the repo root and are tracked; only the per-machine state under .claude/ is ignored.
.claude/settings.local.json
.claude/scheduled_tasks.lock

# corpus-specific eval gold set (per-machine)
eval_queries.json
